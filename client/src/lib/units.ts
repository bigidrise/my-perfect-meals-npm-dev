export const lbToKg = (lb: number) => +(lb * 0.45359237).toFixed(1);
export const kgToLb = (kg: number) => +(kg / 0.45359237).toFixed(1);
export const inToCm = (inch: number) => +(inch * 2.54).toFixed(1);
export const cmToIn = (cm: number) => +(cm / 2.54).toFixed(1);