Here's the complete implementation summary you can copy to your production agent:

Copilot Honest Lighting Implementation Guide
File Modified
client/src/components/copilot/CopilotSheet.tsx

CHANGE 1: Add Import
At the top of the file, add this import:

import { isDoItYourselfMode } from "./CopilotRespectGuard";
CHANGE 2: Add State/Effect
Right after the existing hooks at the top of the component (after the useCopilotGuidedMode() line), add:

// Check if auto-open is truly armed (autoplay ON + not in DIY mode)
const [isAutoArmed, setIsAutoArmed] = useState(false);
useEffect(() => {
  // Client-side check to avoid hydration issues
  const armed = isGuidedModeEnabled && !isDoItYourselfMode();
  setIsAutoArmed(armed);
}, [isGuidedModeEnabled, isOpen]);
CHANGE 3: Update AUTO Label Styling
Find the line:

<span className="text-[9px] text-white/50">Auto</span>
Replace with:

<span className={`text-[9px] transition-all duration-200 ${
  isAutoArmed 
    ? "text-emerald-400 drop-shadow-[0_0_4px_rgba(52,211,153,0.5)]" 
    : "text-white/50"
}`}>Auto</span>
CHANGE 4: Update OFF Button Styling
Find the OFF state styling (the else branch in the button className):

: "bg-white/8 text-white/50 shadow-[0_1px_2px_rgba(0,0,0,0.3)] hover:bg-white/12 border border-white/15"
Replace with:

: "bg-amber-500/20 text-amber-200 shadow-[0_1px_2px_rgba(0,0,0,0.3)] hover:bg-amber-500/30 border border-amber-400/40"
Troubleshooting Checklist
Verify isDoItYourselfMode exists in CopilotRespectGuard.ts - if not, check how that file exports its functions
Rebuild required - run npm run build before deploying
Clear iOS WKWebView cache - force close app completely and reopen
Check browser cache - hard refresh (Cmd+Shift+R) on deployed URL
Expected Behavior After Fix
State	AUTO Label	Button
Autoplay ON + Guided Mode	Green glow	Green "On"
Autoplay ON + DIY Mode	Muted gray	Green "On"
Autoplay OFF	Muted gray	Amber "Off"
Copy this entire block to your production agent. They should search for the exact strings mentioned and make the replacements.