Got it. I’ll **add Wedding Cake + cake styles (including Naked Cake)** cleanly **without breaking your 5-field structure** or overcomplicating the UX.

Below is the **exact, minimal change set** you want. This is surgical, not a rewrite.

---

## 1️⃣ Add Wedding Cake as a first-class option (correct place)

### ✅ Where it belongs

**Dessert Category = Cake**
**Specific Dessert = Wedding Cake**

You do **not** create a new category. Wedding cake is a *cake use-case*, not a dessert type.

### 🔧 Add this helper list (near constants)

```ts
const CAKE_SPECIALTIES = [
  { value: "wedding-cake", label: "Wedding Cake" },
  { value: "birthday-cake", label: "Birthday Cake" },
  { value: "celebration-cake", label: "Celebration Cake" },
];
```

---

## 2️⃣ Add Cake Style (Naked Cake, Semi-Naked, Classic)

This is the key upgrade.

### 🔧 New constant

```ts
const CAKE_STYLES = [
  { value: "classic", label: "Classic Frosted" },
  { value: "semi-naked", label: "Semi-Naked (Light Frosting)" },
  { value: "naked", label: "Naked Cake (Minimal Frosting)" },
];
```

---

## 3️⃣ Add state (minimal, safe)

```ts
const [cakeStyle, setCakeStyle] = useState("classic");
```

---

## 4️⃣ Conditionally show Cake Style UI (only when Cake is selected)

Drop this **right after Dessert Category** in the form:

```tsx
{dessertCategory === "cake" && (
  <div>
    <label className="block text-md font-medium text-white mb-1">
      Cake Style
    </label>
    <Select value={cakeStyle} onValueChange={setCakeStyle}>
      <SelectTrigger className="w-full text-sm bg-black text-white border-white/30">
        <SelectValue placeholder="Select cake style" />
      </SelectTrigger>
      <SelectContent>
        {CAKE_STYLES.map((style) => (
          <SelectItem key={style.value} value={style.value}>
            {style.label}
          </SelectItem>
        ))}
      </SelectContent>
    </Select>
  </div>
)}
```

UX result:

* Users **only see this when Cake is chosen**
* Naked Cake feels intentional, not bolted on

---

## 5️⃣ Add Wedding Cake selector (optional but clean)

Replace your **Additional Flavor Notes** label with smarter behavior:

```tsx
{dessertCategory === "cake" && (
  <div>
    <label className="block text-md font-medium text-white mb-1">
      Cake Type (optional)
    </label>
    <Select
      value={specificDessert}
      onValueChange={setSpecificDessert}
    >
      <SelectTrigger className="w-full text-sm bg-black text-white border-white/30">
        <SelectValue placeholder="Select cake type or leave blank" />
      </SelectTrigger>
      <SelectContent>
        {CAKE_SPECIALTIES.map((c) => (
          <SelectItem key={c.value} value={c.label}>
            {c.label}
          </SelectItem>
        ))}
      </SelectContent>
    </Select>
  </div>
)}
```

Users can still type custom text if they want — you didn’t lose flexibility.

---

## 6️⃣ Send style + wedding logic to the API (critical)

Update your API payload:

```ts
body: JSON.stringify({
  dessertCategory,
  flavorFamily,
  specificDessert,
  cakeStyle: dessertCategory === "cake" ? cakeStyle : undefined,
  servingSize,
  dietaryPreferences: [
    ...(dietaryPreference && dietaryPreference !== "none"
      ? [dietaryPreference]
      : []),
    ...(customDietary.trim() ? [customDietary.trim()] : []),
  ],
  userId: DEV_USER_ID,
}),
```

---

## 7️⃣ What the AI should now understand (behavior contract)

When:

* `specificDessert === "Wedding Cake"`
* `cakeStyle === "naked"`

The AI should:

* Reduce frosting volume
* Favor fruit, mascarpone, yogurt-cream fillings
* Output **per-slice nutrition**
* Reference **event-scale servings** (e.g. 60–120 servings)
* Avoid “diet language”

This is not UI — this is **brand intelligence**.

---

## 🔥 Why this is the right design

* No new creator
* No bloated form
* Naked Cake feels *premium*, not gimmicky
* Wedding cake becomes a **celebration-grade output**, not a joke dessert

You just turned Dessert Creator into an **event-level culinary tool**.

If you want next:

* I can write the **exact system prompt logic** for wedding + naked cakes
* Or add a **“Per Slice vs Whole Cake” macro display** (huge win)

Say the word.
