Your task: FIX the Copilot intent system inside CopilotCommandRegistry.ts so that:

1. ANY keyword inside a user query triggers the correct feature.
2. The Copilot responds with the correct explanation (knowledge card or walkthrough).
3. AND the Copilot automatically navigates the user to that feature page in the app.

This Copilot must:
- Hear the keyword
- Explain the feature in text
- AND open the correct page automatically

=====================================
REQUIRED IMPLEMENTATION DETAILS
=====================================

Modify ONLY the dispatcher logic inside handleVoiceQuery(). 
Do NOT change UI components, pages, registries, or voice transcription.

-------------------------------------
1. Add a fuzzy keyword matcher
-------------------------------------

Create a KEYWORD_ROUTES map with these triggers:

Weekly Meal Board:
- "weekly", "planner", "meal board", "weekly board", "plan meals"

Diabetic Hub:
- "diabetic", "diabetes", "blood sugar"

GLP-1 Hub:
- "glp", "glp1", "glp-1"

Beach Body Builder:
- "beach", "beach body", "lean mode", "summer shred"

Alcohol Hub:
- "alcohol", "drink", "drinks", "social"

Fridge Rescue:
- "fridge", "rescue", "fridge rescue"

Macro Calculator:
- "macro", "macros", "macro calculator", "track macros"

Biometrics:
- "biometric", "tracker", "progress", "track body"

Restaurant Guide:
- "restaurant", "restaurants", "eat out", "going out"

Find Meals Near Me:
- "find meals", "meals near me", "nearby food", "meal finder"

-------------------------------------
2. Fuzzy matching rules
-------------------------------------

Normalize the input:
- lowercase
- strip punctuation

For each KEYWORD_ROUTES entry:
- If the user's message CONTAINS the keyword → consider it a match.

-------------------------------------
3. When a feature is matched:
-------------------------------------

Do ALL of the following (in this order):

A. RETURN a Copilot text explanation  
   using explainFeature(featureId) or WalkthroughRegistry.

B. EXECUTE the corresponding navigation action  
   using CopilotAction:  
   runAction({ type: "navigate", to: "/correct-route" });

C. NEVER return “still learning” after a match.

-------------------------------------
4. Only return “still learning” IF:
-------------------------------------

- No keywords matched  
AND  
- No knowledge article matched  
AND  
- No walkthrough matched

-------------------------------------
5. DO NOT MODIFY:
-------------------------------------

- AppKnowledgeRegistry.ts
- WalkthroughRegistry.ts
- Any UI components
- Any router files
- Any pages
- Voice transcription endpoint

Modify ONLY handleVoiceQuery AND the dispatcher logic to add:
- fuzzy matching
- knowledge fallback
- walkthrough fallback
- AND automatic navigation.

-------------------------------------
SUCCESS CRITERIA:
-------------------------------------

EXAMPLES:

User says: “Help me with diabetic meals”
→ Copilot explains Diabetic Hub  
→ Copilot navigates user to /diabetes-hub

User says: “Where’s the weekly planner?”
→ Copilot explains Weekly Meal Board  
→ Copilot opens /planner or /weekly-meal-board

User says: “I want beach body meals”
→ Copilot explains Beach Body Builder rules  
→ Copilot navigates to /beach-body

User says: “What are macros?”
→ Copilot explains Macro Calculator  
→ Copilot opens /macros

The Copilot must ALWAYS:
- EXPLAIN the feature  
- AND open the feature page automatically
