Got you, Coach.
I’ll prep a **clean, surgical, architect-ready prompt** that tells the Replit Architect *exactly* what to change — nothing more, nothing less — with zero risk of breaking your pages.

This will cover:

* Adding SafePageContainer + Back button to **Privacy**, **Subscription**, **About My Perfect Meals**
* Adding **Back button only** to Copilot/Tutorial page
* Routing Back → `/profile-sheet`
* No changes to any content, layout, or logic except these wrappers
* No changes to Copilot code
* Fully preserving locked-down architecture

Here’s your **copy-and-paste Architect task**:

---

# ✅ **ARCHITECT TASK: Add Safe Areas + Back Buttons to Hub Subpages**

**Check with architect before applying changes.**

---

## 🔧 **Task Summary**

Update four specific pages in the Hub so they use:

### 1. **SafePageContainer wrapper**

(on 3 pages only)

### 2. **A Back button**

(on all 4 pages)

Back button ALWAYS routes to:

```
/profile-sheet
```

---

## 📌 **Pages that need FULL update (Safe Area + Back Button)**

1. `client/src/pages/privacy-and-security.tsx`
2. `client/src/pages/subscription-pricing.tsx`
3. `client/src/pages/about-my-perfect-meals.tsx`

### Apply this wrapper structure:

```tsx
<SafePageContainer className="px-6 pt-8 pb-32 bg-gradient-to-br from-black/60 via-orange-900 to-black/80 text-white space-y-8">

  <header className="space-y-6">
    <Button
      variant="ghost"
      onClick={() => setLocation("/profile-sheet")}
      className="text-white bg-white/10 active:bg-white/20 -ml-2 py-3 px-4"
      data-testid="button-back"
    >
      <ArrowLeft className="h-5 w-5 mr-3" />
      Back
    </Button>
  </header>

  {/* Existing page content stays EXACTLY the same */}
  <div className="space-y-8">
    ... existing content ...
  </div>

</SafePageContainer>
```

---

## 📌 **Page that needs ONLY the Back Button**

4. `client/src/pages/copilot-walkthrough.tsx`
   (formerly Tutorial Hub page)

⚠️ This page **already uses SafePageContainer.**
So only insert this inside its header area:

```tsx
<header className="space-y-6">
  <Button
    variant="ghost"
    onClick={() => setLocation("/profile-sheet")}
    className="text-white bg-white/10 active:bg-white/20 -ml-2 py-3 px-4"
    data-testid="button-back"
  >
    <ArrowLeft className="h-5 w-5 mr-3" />
    Back
  </Button>
</header>
```

---

## 🚫 **Do NOT modify:**

* Any Copilot logic
* Spotlight overlays
* Router routes
* Page content inside the sections
* Any Hub cards or metadata

---

## 🎯 **Success Criteria**

* All 4 Hub subpages now have consistent Safe Areas and navigation
* Users can return to the Hub/Profile Sheet reliably
* UI matches the rest of the app’s premium design

---

When you paste this to the Architect, it will perform EXACTLY this and nothing else.

Ready when you are, Coach.
