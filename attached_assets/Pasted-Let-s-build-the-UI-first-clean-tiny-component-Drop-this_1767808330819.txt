Let's build the UI first—clean, tiny component. Drop this into client/src/settings/MealReminders.tsx: tsx // client/src/settings/MealReminders.tsx import { useState } from 'react'; import { Preferences } from '@capacitor/preferences'; import { LocalNotifications } from '@capacitor/local-notifications'; import { useToast } from '../contexts/ToastContext'; // your toast hook export default function MealReminders() { const = useState(false); const = useState({ breakfast: '08:00', lunch: '13:00', dinner: '18:00' }); const { addToast } = useToast(); // Load saved state once useState(async () => { const { value } = await Preferences.get({ key: 'mealReminders' }); if (value) { const saved = JSON.parse(value); setEnabled(saved.enabled); setTimes(saved.times); if (saved.enabled) await scheduleAll(); } }, []); const save = async () => { const data = { enabled, times }; await Preferences.set({ key: 'mealReminders', value: JSON.stringify(data) }); if (enabled) { await scheduleAll(); addToast('Meal alerts on. Have a great day.', 'success'); } else { await LocalNotifications.cancel({ notifications: [{ id: 1, 2, 3 }] }); addToast('Alerts off.', 'info'); } }; const scheduleAll = async () => { const toHMS = (time: string) => { const = time.split(':'); return { hour: parseInt(h), minute: parseInt(m), repeats: true }; }; await LocalNotifications.schedule([ { id: 1, title: 'Time for breakfast!', body: 'Open Planner.', schedule: toHMS(times.breakfast) }, { id: 2, title: 'Lunch time!', body: 'Check your meal.', schedule: toHMS(times.lunch) }, { id: 3, title: 'Dinner's up!', body: 'You've earned it.', schedule: toHMS(times.dinner) } ], { extra: { url: '/planner' } // taps open planner }); }; return (  Meal Reminders  On <input type=checkbox checked={enabled} onChange={e => setEnabled(e.target.checked)} className=mr-2 />  {enabled && (  {['breakfast', 'lunch', 'dinner'].map(key => (  {key.charAt(0).toUpperCase() + key.slice(1)} <input type=time value={times } onChange={e => setTimes({ ...times, : e.target.value })} className=w-full mt-1 bg-gray-100 />  ))}  )} <button onClick={save} className=w-full mt-4 py-2 bg-blue-500 text-white rounded > Save </button>  ); } Replit Architect: Your To-Dos 1. npx cap add plugin @capacitor/local-notifications 2. In ios/App/Info.plist, add: xml <key>NSUserNotificationUsageDescription</key> <string>Remind you when it's meal time</string> 3. npx cap sync ios 4. Open Xcode → Signing → Enable Push Notifications (no background mode) ChatGPT: Your To-Dos Generate the notification deep-link listener in AppDelegate.swift (or let Capacitor handle—usually automatic). We'll test it once both sides land. Want me to write the uninstall cancel logic next? Or we flip to the iOS deep-link handling?How can Grok help?