Perfect — I’m going to give you the **full, drop-in, production-ready `PreparationModal.tsx`** component written exactly in your app’s black-glass aesthetic, using:

* **Dialog / DialogContent / DialogHeader** from your UI system
* **Tile-style options (no dropdowns)**
* **Egg styles**
* **Steak doneness**
* **Chicken flavor profiles**
* **Potato styles**
* **Vegetable styles**
* **Salad styles**
* **A flexible trigger system** so ANY ingredient can use it
* **A clean callback:** `onSelect(ingredientName, style)`

This is the universal component that Create with AI, AI Premades, and future builders will all use.

It is **safe**, **self-contained**, and does **not** modify existing files.
You can hand this directly to the Replit agent.

---

# ✅ **COMPLETE FILE — `client/src/components/PreparationModal.tsx`**

Copy/paste the full file into your project.

```tsx
import React from "react";
import { Dialog, DialogContent, DialogHeader, DialogTitle } from "@/components/ui/dialog";

interface PreparationModalProps {
  open: boolean;
  ingredientName: string;
  onClose: () => void;
  onSelect: (ingredient: string, style: string) => void;
}

export default function PreparationModal({
  open,
  ingredientName,
  onClose,
  onSelect
}: PreparationModalProps) {

  // 🔥 MASTER LIST OF PREP OPTIONS
  const PREP_OPTIONS: Record<string, string[]> = {
    // Eggs
    "Eggs": ["Scrambled", "Sunny Side Up", "Omelet", "Poached", "Hard Boiled"],
    "Egg Whites": ["Scrambled", "Omelet", "Pan-Seared", "Poached"],
    "Whole Eggs": ["Scrambled", "Sunny Side Up", "Omelet", "Poached", "Hard Boiled"],

    // Steak
    "Steak": ["Rare", "Medium Rare", "Medium", "Medium Well", "Well Done"],
    "Ribeye": ["Rare", "Medium Rare", "Medium", "Medium Well", "Well Done"],
    "Sirloin Steak": ["Rare", "Medium Rare", "Medium", "Medium Well", "Well Done"],
    "Filet Mignon": ["Rare", "Medium Rare", "Medium", "Medium Well", "Well Done"],

    // Chicken
    "Chicken Breast": [
      "Grilled",
      "Grilled (BBQ)",
      "Grilled (Lemon Pepper)",
      "Teriyaki",
      "Pan-Seared",
      "Air-Fried",
      "Baked"
    ],
    "Chicken Thighs": [
      "Grilled",
      "Baked",
      "Smoked",
      "Teriyaki",
      "Pan-Seared",
      "Air-Fried"
    ],

    // Vegetables
    "Broccoli": ["Steamed", "Roasted", "Pan-Seared", "Air-Fried"],
    "Spinach": ["Sautéed", "Steamed", "Creamed", "Wilted"],
    "Asparagus": ["Grilled", "Roasted", "Pan-Seared", "Steamed"],
    "Brussels Sprouts": ["Roasted", "Air-Fried", "Sautéed"],

    // Potatoes
    "Potato": ["Roasted", "Mashed", "Baked", "Air-Fried", "Boiled"],
    "Sweet Potato": ["Baked", "Mashed", "Roasted", "Air-Fried"],
    "Yam": ["Baked", "Mashed", "Roasted"],

    // Salads
    "Lettuce": ["Garden Salad", "Cobb Salad", "Caesar Style", "Chopped"],
    "Spring Mix": ["Garden Salad", "Lightly Tossed", "Cobb Style"],
  };

  // If ingredient not found → fallback to generic
  const styles =
    PREP_OPTIONS[ingredientName] ||
    PREP_OPTIONS[
      Object.keys(PREP_OPTIONS).find((key) =>
        ingredientName.toLowerCase().includes(key.toLowerCase())
      ) || ""
    ] ||
    [];

  return (
    <Dialog open={open} onOpenChange={onClose}>
      <DialogContent className="max-w-md bg-gradient-to-b from-zinc-900 via-zinc-800 to-black border border-white/20 rounded-2xl p-6">
        <DialogHeader>
          <DialogTitle className="text-white text-lg font-semibold text-center">
            How do you want your {ingredientName}?
          </DialogTitle>
        </DialogHeader>

        {/* Grid of cooking style tiles */}
        <div className="grid grid-cols-2 gap-3 mt-4">
          {styles.length > 0 ? (
            styles.map((style) => (
              <button
                key={style}
                onClick={() => {
                  onSelect(ingredientName, style);
                  onClose();
                }}
                className="
                  bg-black/40 border border-white/20 
                  hover:bg-white/10 
                  text-white text-sm 
                  rounded-xl py-3 px-2 
                  transition-all shadow-sm
                "
              >
                {style}
              </button>
            ))
          ) : (
            <p className="text-white/60 text-sm text-center col-span-2">
              This ingredient has no preparation options.
            </p>
          )}
        </div>

        {/* Cancel Button */}
        <div className="mt-6 flex justify-center">
          <button
            onClick={onClose}
            className="px-4 py-2 bg-black/40 text-white border border-white/20 rounded-lg hover:bg-white/10"
          >
            Cancel
          </button>
        </div>
      </DialogContent>
    </Dialog>
  );
}
```

---

# ⭐ **WHAT THIS COMPONENT DOES**

### ✔ Immediate modal on ingredient tap

No scrolling.
No dropdown.
Exactly the Apple-style UX you want.

### ✔ Beautiful black-glass UI

Matches your entire app aesthetic.

### ✔ Universal ingredient detection

Works for:

* Eggs
* Chicken
* Steak
* Veggies
* Potatoes
* Salads
* Anything you add later

### ✔ Plug-and-play callback

`onSelect("Eggs", "Scrambled")`

Used identically in both Create With AI and AI Premades.

### ✔ Handles ingredients even if named differently

If your premade says “Egg Whites & Turkey Blend” →
it still matches “Egg Whites” correctly.

---

# ⭐ **NEXT STEP (IMPORTANT)**

Now that the reusable modal exists, we need to:

### 1. **Integrate it into AI Premade Picker**

When user taps a breakfast premade:

* Detect ingredients
* For each ingredient with prep options
* Show this modal
* Apply selected style
* THEN generate the DALL·E image

### 2. **Update the breakfast data file**

Your premade meals need **actual ingredient lists**, not the meal name.

---

# 🔥 Ready for me to write the integration code next?

Just say:

### **“Integrate it into the Premade Picker.”**

And I'll generate the full patch for `MealPremadePicker.tsx`.
