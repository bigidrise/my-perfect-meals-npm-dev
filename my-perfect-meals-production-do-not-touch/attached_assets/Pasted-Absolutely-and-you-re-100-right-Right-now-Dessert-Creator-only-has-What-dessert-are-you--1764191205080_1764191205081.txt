Absolutely — and you’re **100% right**.
Right now Dessert Creator only has:

* What dessert are you craving? (free-text)
* Flavor / texture vibe (free-text)
* Servings (dropdown)
* Dietary (free-text)

**There is NO dropdown telling the AI *what type of dessert* to generate.**

And that *matters*, because:

* “Chocolate craving” + **cake** → Chocolate Lava Cake
* “Chocolate craving” + **pie** → Chocolate Silk Pie
* “Chocolate craving” + **smoothie** → Chocolate Protein Smoothie
* “Chocolate craving” + **cookie** → Giant Chewy Chocolate Chip Cookie
* “Chocolate craving” + **pudding** → High-Protein Chocolate Pudding

The user deserves control over that.

Time to add the missing dropdown.

---

# ✅ We add this new field:

**Dessert Type:**

* Pie
* Cake
* Cookie
* Brownie
* Cheesecake
* Smoothie
* Frozen Treat
* Pudding / Custard
* No-Bake Dessert
* Bars
* Muffins
* Cupcakes
* Pastry
* “Surprise Me” (AI decides)

---

# 🔥 Step 1 — I’ll give you the **updated frontend snippet** for the dropdown

(You will paste this inside the `<CardContent>` where the other inputs are.)

**Paste this EXACTLY where the other form controls are:**

```tsx
{/* Dessert Type */}
<div>
  <label className="block text-xs font-medium mb-1 text-white">
    Dessert Type
  </label>
  <Select value={dessertType} onValueChange={(v) => setDessertType(v)}>
    <SelectTrigger className="w-full text-sm bg-black text-white border-white/30">
      <SelectValue placeholder="Select dessert type" />
    </SelectTrigger>

    <SelectContent>
      <SelectItem value="pie">Pie</SelectItem>
      <SelectItem value="cake">Cake</SelectItem>
      <SelectItem value="cookie">Cookie</SelectItem>
      <SelectItem value="brownie">Brownie</SelectItem>
      <SelectItem value="cheesecake">Cheesecake</SelectItem>
      <SelectItem value="smoothie">Smoothie</SelectItem>
      <SelectItem value="frozen">Frozen Dessert</SelectItem>
      <SelectItem value="pudding">Pudding / Custard</SelectItem>
      <SelectItem value="nobake">No-Bake Dessert</SelectItem>
      <SelectItem value="bars">Bars (brownie-style)</SelectItem>
      <SelectItem value="muffin">Muffins</SelectItem>
      <SelectItem value="cupcake">Cupcakes</SelectItem>
      <SelectItem value="pastry">Pastry</SelectItem>
      <SelectItem value="surprise">Surprise Me</SelectItem>
    </SelectContent>
  </Select>
</div>
```

---

# 🔥 Step 2 — Add this to your component state:

Right at the top with your other `useState` calls:

```tsx
const [dessertType, setDessertType] = useState("");
```

---

# 🔥 Step 3 — Pass dessertType to backend:

Inside your `fetch("/api/meals/dessert-creator")` body:

```ts
body: JSON.stringify({
  dessert,
  flavor,
  dietary,
  servings,
  dessertType,
  userId: DEV_USER_ID,
})
```

---

# 🔥 Step 4 — Update backend to use this

Inside `server/routes/dessert-creator.ts`, update the prompt section:

Find:

```ts
- Dessert requested: "${dessert}"
- Flavor/texture vibe: "${flavor}"
- Dietary requirements: "${dietary}"
- Number of servings: ${servings}
```

Replace with:

```ts
- Dessert requested: "${dessert}"
- Dessert TYPE: "${dessertType}"
- Flavor/texture vibe: "${flavor}"
- Dietary requirements: "${dietary}"
- Number of servings: ${servings}
```

AND update the destructuring at the top:

```ts
const { dessert, flavor, dietary, servings, dessertType, userId } = req.body;
```

---

# ⚡ Result

Now the user can say:

> “I’m craving something lemony and fluffy — make it a pie.”

Boom → Lemon Cream Protein Pie.

Or:

> “I want something chocolatey and gooey — cookie mode.”

Boom → High-Protein Double-Chocolate Skillet Cookie.

Dessert Creator becomes a **true dessert builder**, not just dessert freestyle.

---

## If you want, I can rebuild your whole Dessert Creator front-end file with the dropdown integrated cleanly.

Just say **“rebuild the full file”**.
